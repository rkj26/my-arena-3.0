So, when you think about misalignment we get into a thing called reward misspecification or specification gaming where the model basically tries to meet the objective but not the true value. and some examples of this right off the bat is which one deep mine example was given was stacking of lego blocks where the reward function was that the bottom so the problem was stack a red block on top of a blue block and the reward function was if the bottom of the red block matches the top of the blue block it's a match and the thing which ended up happening is that the AI agent just learned to flip the red block rather than doing the actual thing. That's basically what misalignment does. Similarly, one way people think you can fix this is by human feedback, so let the human tell if that's right or wrong. The issue here is that there was another example where it was about gripping the ball. What the AI did was it placed itself in such a way that it fooled the humans into thinking that it was actually gripping the ball. so another form of misalignment this basically means that we need to have reward functions which are very not leaky otherwise we might have a situation of misalignment as capabilities increase and these AMLs become more powerful and that's basically where the concerns of AI safety come into play